import 'package:flutter/material.dart';

const primaryColor = Color.fromRGBO(4, 4, 4, 0.808);
const dimGreyColor = Color.fromRGBO(239, 233, 233, 0.7);
const redColorDark = Color.fromRGBO(227, 27, 40, 0.8);
const dividerColor = Colors.grey;
const whiteColor = Colors.white;