name: narrator
description: "A new Flutter project."

publish_to: 'none' # Prevent accidental publishing

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2  # For API calls
  flutter_tts: ^3.6.3  # For text-to-speech
  just_audio: ^0.9.7  # For audio playback
  cupertino_icons: ^1.0.8  # iOS-style icons
  dio: ^4.0.6
  speech_to_text: ^5.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true